# Secrets
.env

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/

# Uploads (keep the placeholder)
uploads/*
!uploads/.gitkeep

# Test / coverage
.pytest_cache/
.coverage
htmlcov/

# Downloaded ONNX embedding model + tokenizer assets (fetched by
# scripts/download_embedding_model.py; not committed, ~35MB binary)
models/

# Alembic
# (versions/ IS committed — migrations are source-controlled)

# IDE
.vscode/
.idea/
*.iml

# Evaluation corpus PDFs. The manifest (corpus/manifest.json) IS committed —
# it carries the URL, SHA-256 and licence for each document, and
# `python -m evaluation.download_corpus` reproduces this directory from it.
# The files themselves are ~95MB of third-party publications.
evaluation/corpus/files/
