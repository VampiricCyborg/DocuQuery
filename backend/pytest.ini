[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    integration: runs against a real PostgreSQL database with pgvector. Requires
        TEST_DATABASE_URL; skipped automatically when it is unset. Select with
        `-m integration`, exclude with `-m "not integration"`.
