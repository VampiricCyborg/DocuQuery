"use client"
import { ChatWindow } from "@/components/chat/ChatWindow"

/** A new, unsaved chat. The server creates the conversation on the first message. */
export default function ChatPage() {
  return <ChatWindow />
}
